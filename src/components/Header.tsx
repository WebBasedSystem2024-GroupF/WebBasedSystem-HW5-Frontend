import React from 'react';

const Header: React.FC = () => {
  return (
    <header className="header">
      <div className="logo">POI Recommendation</div>
    </header>
  );
};

export default Header;